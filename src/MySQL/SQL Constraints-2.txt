CREATE TABLE IF NOT EXISTS updates (
	id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	user_id INT NOT NULL,
	about TEXT NOT NULL,
	posted TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
	CONSTRAINT __user_uid FOREIGN KEY (user_id)
		REFERENCES users(id)
			ON DELETE CASCADE
			ON UPDATE CASCADE
);